# Flows

1. Create a routine with trigger, concurrency, catch-up, workflow, and job metadata.
2. Update the routine lifecycle or cadence.
3. Trigger the routine and persist queue state, workflow state, and operator inbox context.
